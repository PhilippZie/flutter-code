import 'package:flutter/material.dart';

class WasserverbrauchPage extends StatelessWidget {
  const WasserverbrauchPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Row()],
    );
  }
}
