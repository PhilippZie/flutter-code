import 'package:flutter/material.dart';

class GasverbrauchPage extends StatelessWidget {
  const GasverbrauchPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Row()],
    );
  }
}
